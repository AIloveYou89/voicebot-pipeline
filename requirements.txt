# Core server
flask
flask-cors
flask-sock

# ML / inference
numpy>=2.0,<3
torch
torchaudio
transformers
accelerate
faster-whisper
soundfile
scipy>=1.13

# TTS (F5-TTS-Vietnamese) — install local fork: pip install -e /workspace/F5-TTS-Vietnamese
# f5-tts  # DO NOT install from PyPI, use local fork instead
vocos
librosa
pydub

# API fallback
openai

