# Core ML
transformers==4.46.2

# Quantization (Qwen2.5-7B-AWQ)
autoawq==0.2.7

# Audio I/O
soundfile
numpy>=2.0.2
soxr
scipy
librosa

# SparkTTS dependencies (kept for fallback)
accelerate>=1.2.0
einops>=0.8.1
einx>=0.3.0
safetensors>=0.5.2

# VieNeu-TTS (default engine)
vieneu
phonemizer>=3.3.0
neucodec>=0.0.4

# Text preprocessing
num2words

# RunPod serverless
runpod==1.7.9

# HuggingFace model download
huggingface_hub
